# Fixtures

Example JSONL fixture sets for `@siftline/core` live here — the inputs a recipe is
measured against.

Nothing is here yet: the Engine lands after this scaffold. Fixtures are test data, not
published output, so this directory stays out of `files`.
