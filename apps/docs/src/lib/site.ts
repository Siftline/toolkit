/** What this site is called, in the nav and anywhere else the product is named. */
export const appName = "Siftline";

/** The URL prefix every documentation page hangs off — the source loader's `baseUrl`. */
export const docsRoute = "/docs";
