---
---

Scaffold the workspace, the three published packages, the docs site and the
release pipeline. No version bump: `@siftline/core` `0.0.2` and `@siftline/cli`
and `@siftline/actions` `0.0.1` are the versions the one-time manual first
publish uploads, so the first `changeset version` run must not move them.
